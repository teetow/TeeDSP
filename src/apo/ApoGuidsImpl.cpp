// Define all GUIDs declared in ApoGuids.h.
// INITGUID must be defined before any GUID header so DEFINE_GUID emits a
// real variable definition instead of an extern declaration.
#define INITGUID
#include <guiddef.h>
#include "ApoGuids.h"
